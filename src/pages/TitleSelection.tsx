import TitleSelection from '../TitleSelection/TitleSelection';

function TitleSelectionPage() {
    return (
        <div>
            <TitleSelection />
        </div>
    );
}

export default TitleSelectionPage;
