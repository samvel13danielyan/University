import Header from "../common/Header";
import NavBar from "../common/Navbar";

function Events() {
    return (
        <>
            <Header />
            <NavBar />
        </>
    );
}

export default Events;
